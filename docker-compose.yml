

services:
  postgres:
    image: postgres:16-alpine
    container_name: jobseek-postgres
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-jobseek}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-jobseek_secret}
      POSTGRES_DB: ${POSTGRES_DB:-jobseek_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-jobseek}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local
